{
  "name": "omp-plugins",
  "private": true,
  "version": "0.0.0",
  "description": "Personal omp plugins monorepo",
  "type": "module",
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "check": "tsc --noEmit && oxlint",
    "fmt": "oxfmt",
    "test": "vitest"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "latest",
    "bun-types": "^1.3.14",
    "oxlint": "^1.77.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
